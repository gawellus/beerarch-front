import { RankColorPipe } from './rank-color.pipe';

describe('RankColorPipe', () => {
  it('create an instance', () => {
    const pipe = new RankColorPipe();
    expect(pipe).toBeTruthy();
  });
});
